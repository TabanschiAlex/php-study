<?php
    $eight = 0123;
    $dec = 123;
    $sixt = 0x1A;
    $binary = 0b11111;

    var_dump($eight, $dec, $sixt, $binary);

    var_dump(round(5 / 2));
    var_dump(intdiv(5, 2));
    var_dump(5 / 2);
    var_dump((int) (25 / 3.5));